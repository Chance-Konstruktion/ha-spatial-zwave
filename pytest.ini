[pytest]
testpaths = tests
# The suite imports the integration and the conformance kit by name, and
# neither is installed -- this repository is copied into a Home Assistant
# config, not pip-installed. Naming the two roots here keeps that out of
# conftest.py, where it was a sys.path edit that ran on import.
pythonpath = . tests
